workflows:
  ios-workflow:
    name: GifWave iOS Build
    max_build_duration: 60
    environment:
      xcode: latest
      node: 18
      vars:
        VITE_SUPABASE_URL: https://nwdwfwokdpjdkpsztuuo.supabase.co
        VITE_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im53ZHdmd29rZHBqZGtwc3p0dXVvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzI2NTMzODMsImV4cCI6MjA4ODIyOTM4M30.H9_9Noz8A2kOeP1J2yHP9Vr_06S9T2q0UWdTL3Z1wUo
        VITE_BACKEND_URL: https://gifwave-backend.onrender.com
    scripts:
      - name: Setup
        script: |
          cd frontend
          npm install
          npm install @capacitor/core @capacitor/cli @capacitor/ios
          npm run build

      - name: Setup iOS
        script: |
          cd frontend
          ./node_modules/.bin/cap add ios || true
          # Minimum iOS deployment target ayarla
          sed -i '' "s/platform :ios, '13.0'/platform :ios, '14.0'/" ios/App/Podfile || true
          # Eğer platform satırı yoksa ekle
          grep -q "^platform :ios" ios/App/Podfile || sed -i '' '1s/^/platform :ios, '"'"'14.0'"'"'\n/' ios/App/Podfile
          cat ios/App/Podfile
          ./node_modules/.bin/cap copy ios

      - name: CocoaPods
        script: |
          cd frontend/ios/App
          pod install --repo-update

      - name: Build
        script: |
          xcodebuild \
            -workspace frontend/ios/App/App.xcworkspace \
            -scheme App \
            -configuration Debug \
            -sdk iphoneos \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            CONFIGURATION_BUILD_DIR=$CM_BUILD_DIR/build \
            clean build

    artifacts:
      - $CM_BUILD_DIR/build/*.app
